# IMPORTANT: Set the real backend URL in Vercel's dashboard as an
# environment variable named VITE_API_BASE_URL (this overrides this file
# at build time). Update the value here if building/previewing locally.
VITE_API_BASE_URL=https://grocerypick-api.onrender.com/api
